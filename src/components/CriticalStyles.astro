<style is:inline>
    /* Critical styles for initial render - only include what's needed for LCP */
    .carousel-wrapper {
        position: relative;
        width: 100%;
        height: calc(100vh - 80px);
        margin-top: 80px;
        overflow: hidden;
    }

    .carousel-slide {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    .desktop-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .mobile-image {
        display: none;
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    @media (max-width: 512px) {
        .desktop-image {
            display: none;
        }
        .mobile-image {
            display: block;
        }
    }
</style>
